import 'package:flutter/material.dart';

class SettingArgsPage extends StatelessWidget {
  const SettingArgsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('세팅'),
      ),
      body: Container(

      ),
    );
  }
}
